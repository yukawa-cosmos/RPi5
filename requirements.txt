# ぴっぴ クライアント（ラズパイ / Access）依存パッケージ
#   インストール:  pip install -r client/requirements.txt
#
#   役割: センサー検知・カメラ取得・音声録音/再生・WebSocketクライアント
#         ＋ スタッフ顔照合のみローカル実行（来訪者照合・STT・TTS・AIはクラウド側）。
#   ※ opencv は pip より apt(python3-opencv) が安定（install.sh で対応）。

# --- 通信・音声 I/O（必須・軽量）---
websockets>=12.0
aiohttp
numpy
sounddevice>=0.4.0
soundfile>=0.12.0
python-dotenv==1.2.2
psutil

# --- カメラ・QR ---
opencv-python            # ラズパイは apt: python3-opencv でも可
pyzbar                   # QRコードのデコード

# --- 人物/顔の「検知」（任意・--detector mediapipe を使う場合）---
mediapipe>=0.10.0

# --- スタッフ顔「照合」をローカル実行する場合（設計上の決定 C）---
#   deepface は tensorflow を引き込むため重い。スタッフ即応挨拶が不要なら外してよい。
#   （来訪者照合は常にクラウド側。staff_embeddings.json はクラウドから同期する）
deepface>=0.0.93

# --- ラズパイで PIR / 超音波センサーを使う場合（apt: python3-rpi.gpio 推奨）---
# RPi.GPIO
